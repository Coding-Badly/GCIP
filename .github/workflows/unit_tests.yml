name: unit_tests_for_GCIP
on:
  push:
    paths:
    - "Cargo.lock"
    - "Cargo.toml"
    - "src/**.rs"
    - ".github/workflows/*"
jobs:
  test:
    name: Run unit tests for GCIP.
    runs-on: windows-latest
    strategy:
      matrix:
        rust: ["stable-x86_64-pc-windows-msvc"]
    steps:
    - uses: actions/checkout@master
    - name: Install Rust
      run: rustup update ${{ matrix.rust }}
    - name: Run unit tests
      run: cargo +${{ matrix.rust }} test --verbose
