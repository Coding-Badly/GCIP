[package]
name = "gcip"
version = "0.1.0"
authors = ["Brian Cook <bcook@rowdydog.com>"]
edition = "2018"
build = "build.rs"

[dependencies]
clap = "2.33"
structopt = { version = "0.2", default-features = false }
regex = "1"
toml = "0"  # fix: Move to build-dependencies.

[profile.dev]
panic = "unwind"

[profile.release]
panic = "unwind"

[build-dependencies]
embed-resource = "1.3"

[package.metadata.version]
company-name        = "Rowdy Dog Software"
legal-copyright     = "© Rowdy Dog Software.  All rights reserved."
comments            = "Inserts a pause in G-code generated by KISSlicer for a PolyPrinter."
product-name        = "G-code Insert Pause"
